<div class="container">

  <h1>Add a movie to <%= @list.name %></h1>

  <%= simple_form_for(@bookmark) do |f| %>
    <%= f.input :comment %>
    <%= f.hidden_field :list_id, value: @list.id %>
    <%= f.association :movie %>
    <%= f.submit %>
  <% end %>

</div>
